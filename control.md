# 控制基础：PID、串级环、前馈与调参

> **前置知识**：[overview](./overview.md)（三层架构）、[hardware](./hardware.md)（关节、编码器与力矩）

前几篇已经说明命令怎样到达电机、反馈怎样变回关节状态。真正让机构“按预期运动”的，是这一层控制逻辑。本篇不对应某一个控制器插件，而是把底盘、云台和发射机构反复使用的共同方法集中讲清楚：闭环在闭什么、PID 三项各自解决什么问题、为什么要串级、前馈放在哪里、怎样处理饱和和动态目标，以及应该按什么顺序调车。

这里有一条贯穿全文的边界：**反馈负责纠错，前馈负责提前给出大致正确的控制量，限幅和状态机负责保证错误发生时系统仍然安全。** 三者缺一不可，也不能互相替代。

---

## 1. 先把一个控制回路说完整

最小的闭环只有五个量：

```
目标 r ──► [比较] ──误差 e──► [控制器] ──输出 u──► [机构] ──► 实际值 y
             ▲                                            │
             └──────────────── 传感器反馈 ─────────────────┘
```

- **目标值 $r$**：上层希望得到的状态，例如 3 m/s、0.2 rad、100 rad/s。
- **实际值 $y$**：编码器、IMU 或其他传感器测到的状态。
- **误差 $e=r-y$**：目标和实际之间还差多少。
- **控制量 $u$**：控制器给执行器的命令。在 rm-controls 里，运动控制器最终通常输出关节力矩。
- **被控对象**：电机、减速器、负载和机械结构的总和。负载一变，同一组参数的效果也可能改变。

在 rm-controls 中，这条链通常跨过三层：

```
rm_manual (~100Hz)           rm_controllers (1kHz)             rm_ecat_hw (1kHz)
操作意图 / 模式 ───────────► 目标生成 + 闭环控制 ───────────► 力矩下发
                                  ▲                              │
                                  └──── joint / IMU 反馈 ◄───────┘
```

闭环能否工作，首先取决于四件看起来很基础的事：

1. **量纲一致**：角度统一用 rad，角速度统一用 rad/s，力矩统一用 N·m。不能拿编码器刻度直接减弧度。
2. **符号一致**：正方向必须和 URDF 关节轴、Transmission 减速比及传感器反馈一致。目标为正却向负方向运动时，先查符号，不要反手把 PID 参数调成负数掩盖问题。
3. **周期真实**：离散积分和微分必须使用本次真实的 `period`，不能假定每拍永远恰好 1 ms。
4. **反馈新鲜**：指令或反馈超时后应进入安全态；继续拿旧反馈闭环，等价于闭着眼睛控制。

---

## 2. PID 三项分别在做什么

连续形式的 PID 是：

$$
u(t)=K_p e(t)+K_i\int e(t)\,dt+K_d\frac{de(t)}{dt}
$$

控制器实际按离散周期运行，可把它理解为：

$$
u_k=K_p e_k+K_i\sum_{j=0}^{k}e_j\Delta t+K_d\frac{e_k-e_{k-1}}{\Delta t}
$$

公式不难，难的是知道每一项为什么存在。

### 2.1 P：看现在差多少

比例项 $K_p e$ 直接根据当前误差出力。误差越大，纠正越强。

- $K_p$ 太小：响应迟钝，受恒定负载时会留下明显静差。
- $K_p$ 太大：超调、来回振荡，严重时发散。
- 只有 P 时，系统不一定能精确停在目标上。摩擦、重力等恒定扰动需要一个非零输出抵消，而非零输出又要求保留非零误差。

### 2.2 I：把长期没解决的误差记下来

积分项累加历史误差，专门消除重力、摩擦等持续扰动造成的稳态误差。

- 好处：最终能把静差推近 0。
- 代价：它有记忆。误差长期很大时会积累出很大的控制量，即使目标已经达到，仍要花时间“退积分”，造成明显超调。
- 机构被限位卡住、输出已饱和或电机被禁用时，积分尤其容易失控。

所以 I 从来不是“越大越准”。它应该只补模型没有覆盖掉的慢变化误差。

### 2.3 D：看误差正在怎样变化

微分项根据误差变化率提前刹车，常用于减小超调、增加阻尼。

- $K_d$ 太小：抑制超调的效果不明显。
- $K_d$ 太大：会放大编码器量化和测量噪声，表现为高频抖动或尖锐啸叫。
- 目标阶跃会让误差瞬间变化，普通“误差微分”产生很大的微分冲击。工程中常改为对测量值微分（derivative on measurement），并加低通滤波。

D 不是每个回路都必需。RM 电机速度反馈量化较粗、机械间隙又明显时，一个稳定的 PI 往往比噪声很大的 PID 更实用。

---

## 3. PID 周围必须有的保护

裸公式只适合纸面。能上车的控制器至少还要处理下面几件事。

### 3.1 输出限幅

控制量必须钳在硬件允许范围内：

$$
u=\operatorname{clamp}(u_{PID}+u_{ff},\ u_{min},\ u_{max})
$$

限幅保护电机和机械，但也会改变闭环行为。一旦输出已经顶满，再增大 P 或 I 不会让电机更快，只会让积分积得更深。调参时必须同时画出**未限幅输出、最终输出和饱和标志**，否则很容易误判“参数不够大”。

### 3.2 积分抗饱和

常见做法有三类：

- **积分限幅**：把积分状态限制在固定区间，简单可靠。
- **条件积分**：输出饱和且误差还在把输出推向更深饱和时，暂停积分；误差方向有助于退出饱和时再恢复。
- **反算 anti-windup**：根据“未限幅输出”和“限幅后输出”的差，反向拉回积分状态。

“误差大时积分分离、误差小时恢复积分”也能减少大阶跃造成的积累，但切换阈值需要验证，避免在阈值附近来回跳。

### 3.3 状态切换时清状态

下面这些时刻通常要清积分、清微分历史，并把目标从当前实际值重新接起：

- 控制器启动或停止；
- 遥控器掉线、机构下电；
- 标定控制器与主控制器互相切换；
- 速度模式切到位置模式；
- 指令长时间超时后重新连上。

否则旧模式留下的积分会在新模式第一拍突然释放，出现“重新使能就猛跳一下”。这不是参数问题，是状态生命周期问题。

### 3.4 角度误差要走最短路

连续旋转关节不能直接做普通减法。例如当前角 $0$，目标角 $7\pi/4$，直接相减会逆时针转 $315^\circ$；最短路径其实是顺时针 $45^\circ$。应使用归一化角度误差：

$$
e_\theta=\operatorname{wrapToPi}(\theta_{des}-\theta)\in[-\pi,\pi)
$$

有机械限位的关节则不能只追求“最近”，还要先把候选目标钳进合法区间。舵轮转向还可以把目标方向翻转 $\pi$、同时把驱动轮速度取反，以减少舵向电机转角；这属于机构运动学约束，不应塞进 PID 内部。

---

## 4. 串级控制：先管快变量，再管慢变量

单个 PID 很难同时把位置、速度和力矩都管好。串级控制把问题拆成多层：

```
目标角度
  │
  ▼ 位置环：角度误差 → 目标角速度
目标角速度
  │
  ▼ 速度环：角速度误差 → 目标力矩
目标力矩
  │
  ▼ 电调内部电流环：目标电流/力矩 → PWM
电机与机构
```

内环面对的是变化更快的变量，必须比外环响应快。调参顺序也必须从内向外：

1. 先断开位置环，只调速度环；
2. 让速度环在预期负载下稳定、快速，并设置合理输出限幅；
3. 固定内环参数，再接位置环；
4. 位置环输出的是**目标速度**，它也要限幅，不能要求内环完成不可能的速度阶跃；
5. 最后再加前馈和补偿。

rm-controls 里的典型例子：

| 机构 | 外环 | 内环 | 最终输出 |
| --- | --- | --- | --- |
| 云台 | 角度误差 → 目标角速度 | 角速度误差 → 力矩 | yaw / pitch 力矩 |
| 拨弹盘 | 目标弹位 → 目标运动 | 位置子控制器内部闭环 | trigger 力矩 |
| 底盘轮 | 无统一位置外环 | 轮速误差 → 力矩 | 各轮力矩，再统一做功率限制 |
| 底盘 FOLLOW | 云台-底盘夹角 → 底盘角速度 | 各轮速度环 | 各轮力矩 |

串级环不是“环越多越高级”。只有当内层状态能可靠测量、内环确实更快、每层输出限制清楚时，增加一环才有意义。

---

## 5. 前馈：在误差出现前先出一部分力

反馈的共同限制是：**先有误差，才有纠正。** 对动态目标，这意味着天然滞后。前馈根据目标轨迹或已知扰动直接估计控制量：

$$
u=u_{ff}(r,\dot r,\ddot r,\text{state})+u_{fb}(e)
$$

- $u_{ff}$ 负责让机构大致沿期望轨迹运动；
- $u_{fb}$ 只修模型误差、摩擦变化和外界扰动；
- 前馈准确后，误差会显著变小，PID 不必靠很大的 I 项“追着补”。

在这套笔记里会遇到四类前馈：

1. **速度前馈**：位置目标持续变化时，把目标速度直接送到速度环。云台 RATE 模式尤其依赖它，否则位置环必须保留一个角度误差才能产生持续转速。
2. **加速度/惯量前馈**：根据目标加速度估算 $J\ddot\theta$ 或 $ma$ 所需的力矩，适合轨迹明确的机构。
3. **重力与摩擦补偿**：根据姿态估计重力矩，或根据运动方向估计摩擦力矩。
4. **耦合补偿**：一个机构的运动会扰动另一个机构，例如底盘自转对云台的影响，提前按底盘角速度补偿。

前馈也有明确边界：

- 它依赖模型，负载、摩擦、供电和机械结构一变就会失配；
- 它没有纠错能力，不能替代反馈；
- 用目标差分算速度/加速度时会放大噪声，目标本身要先做轨迹规划或滤波；
- 调前馈前应先保证反馈环稳定，否则很难分辨问题来自模型还是 PID。

实车上常用“物理模型给形状 + 数据拟合参数”的办法：先写出重力的正余弦形式、摩擦的方向形式或功率二次模型，再采集覆盖工作区间的数据，用最小二乘拟合系数。不要直接拿一个高阶多项式把少量数据拟合得很漂亮，却在区间外发散。

---

## 6. 动态目标先规划，再交给闭环

键盘输入只有按下和松开。如果把 0 直接阶跃到 3 m/s，速度环会瞬间要求最大力矩，既冲击机械，也会触发底盘功率限制。最简单的命令整形是斜坡限制：

$$
x_{cmd,k}=x_{cmd,k-1}+\operatorname{clamp}(x_{target}-x_{cmd,k-1},-a_{dec}\Delta t,a_{acc}\Delta t)
$$

其中加速和减速可以使用不同斜率。进一步还要区分两种规划：

- **目标值优先（硬规划）**：只从上一拍规划值向目标靠近。轨迹可预测，但当机构因功率限制跟不上时，规划值会远远跑到实际值前面；操作手松手后仍可能拖很久才降下来。
- **实际值优先（软规划）**：当实际值落在目标与规划值之间时，让规划状态贴回实际值，再从这里继续规划。受限后能更快响应“松手刹车”，手感更直接。

当前 `ChassisBase` 已使用 `RampFilter` 做加减速缓冲。是否要引入实际值感知、S 曲线（限制 jerk）或按超电状态切换加速度，应依据实车冲击、功率和操作手手感决定；这些是命令生成层的扩展，不应通过减小底层 PID 来假装实现。

---

## 7. 一套可复现的调参流程

调参不是盯着“能不能动”，而是比较目标、反馈、误差、输出和限制之间的因果关系。

### 7.1 上车前

1. 架空或固定机构，确认急停有效，设置保守的速度、力矩和关节限位。
2. 确认正方向、单位、减速比、编码器连续性和控制周期。
3. 同时记录 `target`、`measurement`、`error`、PID 各项、前馈、最终输出与饱和标志。
4. 先用小阶跃，确认闭环方向正确；一旦误差增大时输出也在把机构推得更远，立刻停机查符号。

### 7.2 调内环

1. 清零 I、D 和前馈，只增大 P，直到响应足够快但仍有稳定裕量。
2. 小幅增加 I，刚好消除持续负载下的静差；同时设置积分限幅或 anti-windup。
3. 只有存在明确超调、反馈噪声允许时才加 D，并观察高频输出。
4. 在不同转速、正反方向和实际负载下重复测试，不能只看一个工作点。

### 7.3 调外环与前馈

1. 内环固定后再开外环，从小 P 开始，并限制外环输出的最大目标速度。
2. 用静态目标检验定位与抗扰，用斜坡目标检验跟随，用人工轻推检验恢复。
3. 闭环稳定后才加速度、重力、摩擦或耦合前馈。
4. 前馈调到“PID 输出明显变小但仍保留纠错余量”，不要追求 PID 输出永远为 0。
5. 最后测试模式切换、断连重连、上下电、标定结束和限位附近行为。

常见症状可以先这样定位：

| 症状 | 优先检查 | 不要先做什么 |
| --- | --- | --- |
| 一使能就反向加速 | 符号、Transmission、误差定义 | 把 P 改成负数碰运气 |
| 低频大幅摆动 | I 过大、积分饱和、外环过快 | 继续增大 I |
| 高频细抖/啸叫 | P/D 过大、反馈量化、机械间隙 | 用更强 D 压制 |
| 动态目标总落后 | 速度前馈、命令规划、输出饱和 | 只堆积分 |
| 静止目标慢慢下垂 | 重力/摩擦前馈、I 太小 | 盲目增大外环 P |
| 模式切换猛跳 | 目标接续、积分与微分状态清理 | 重调整套 PID |
| 高速时方向偏 | 坐标变换时延、执行器响应、耦合前馈 | 修改静态运动学矩阵凑数 |

---

## 8. 多电机系统多出来的问题

把一个电机复制四份不等于完成多电机控制。还要处理共享资源与一致性：

- **每个执行器有唯一名字和地址**：硬件 ID、actuator、Transmission、joint、YAML 控制器名称必须一一对应。
- **同一时刻的快照**：一拍内应先读取全部反馈，再计算全部控制器，最后统一写命令，避免前两个轮子用了新反馈、后两个用了旧反馈。
- **独立闭环，共享约束**：每个底盘轮各跑自己的速度环；总功率限制在所有轮力矩算完后统一处理。共享限制不应偷偷改某一个 PID 的积分状态。
- **故障不能被平均掉**：同时画每个电机的目标、速度和输出。一个轮子反接、打滑或饱和时，总平均值可能看起来仍然正常。
- **机械耦合要显式建模**：差动、双电机同轴和多自由度机构不能当作多个独立单电机，需要先通过 Transmission 或运动学矩阵解耦。

这正是 rm-controls 把“单关节闭环”“机构运动学”和“整机约束”分层的原因：单个 PID 只解决局部误差，不承担功率分配、碰撞规避或模式决策。

---

## 9. 什么时候需要滤波或观测器

传感器不一定直接给出控制所需的状态。例如电机报文里的整数 rpm 对低速云台过于粗糙，轮速积分又会漂移。可按复杂度逐级处理：

1. 先检查采样、单位、安装与零偏；
2. 再使用低通、滑动窗口等简单滤波；
3. 有多个互补传感器时做融合；
4. 已有可信系统模型、确实存在不可直接测状态时，再考虑龙伯格观测器或卡尔曼滤波。

观测器是“模型 + 测量”的状态估计，不是把曲线变顺眼的滤镜。模型错误、时间戳错位和噪声参数乱填时，估计值可能比原始传感器更危险。部署前必须比较创新量（测量与预测之差）、估计延迟以及传感器失效时的退化行为。

---

## 10. 小结

- 闭环先保证**单位、符号、周期和反馈时效**正确，再谈 PID 参数。
- P 管当前误差，I 消持续静差但会积累，D 提供阻尼但会放大噪声。
- 可上车的 PID 必须配合输出限幅、积分抗饱和、状态切换清理和角度最短路。
- 串级环从内向外调，内环必须更快；外环输出也要有物理限制。
- 前馈按目标和模型提前出力，反馈负责纠错。重力、摩擦、动态目标和机构耦合都适合用前馈描述。
- 动态目标先做斜坡或轨迹规划，不能把键盘阶跃直接砸给底层闭环。
- 多电机控制要把单关节闭环、机构运动学和整机共享约束分开。
- 调参必须记录完整链路，并覆盖模式切换、饱和、断连和真实负载。

带着这些共同概念再读 [transform](./transform.md) 和三个机构文档：[chassis](./chassis.md)、[gimbal](./gimbal.md)、[shooter](./shooter.md)。后面的“速度环”“前馈”“状态清理”和“统一限幅”都会落到具体代码路径上。
